# AE2 Downstream

This mod allows a subnet to connect to the main network, and use the resources of the main network. The main network can't connect to the subnet, so it makes it like a "private network" where you can leech off the main net


TODO:
  - Improve the description, it's so bad
